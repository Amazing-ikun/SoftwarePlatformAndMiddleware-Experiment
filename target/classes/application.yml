spring:
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
    packages:
      trust-all: true

logging:
  level:
    org.springframework.jms: DEBUG
    org.apache.activemq: DEBUG

logistics:
  topic:
    name: WangJingyao.LogisticsTopic

server:
  port: 8080