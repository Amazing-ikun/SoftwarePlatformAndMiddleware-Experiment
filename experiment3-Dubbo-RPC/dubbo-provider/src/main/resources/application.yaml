spring:
  application:
    name: dubbo-provider

server:
  port: 8080

dubbo:
  application:
    name: dubbo-provider
  registry:
    address: nacos://localhost:8848
  protocol:
    name: dubbo
    port: 20880
  scan:
    base-packages: com.example.dubbo.provider.service.impl