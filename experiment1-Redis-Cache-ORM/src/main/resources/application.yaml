server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/redis_orm_demo?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 888@Toki
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  redis:
    host: localhost
    port: 6379
    password: # 如果你的Redis没有设置密码，这里留空即可
    database: 0